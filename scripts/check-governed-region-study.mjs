import{readFileSync}from'node:fs';import{resolve}from'node:path';const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/governed-region-study.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['project_region_sources','project_region_source_events','sync_project_region_source_register','upsert_project_region_source','review_project_region_source','regional_sources_changed','source_url text','valid_until date','review_status text','automaticclaimgeneration','humanreviewed'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);for(const marker of['httpsOnly:true','humanReview:true','validityAware:true','automaticClaims:false','personalDataUsed:false','region_source.reviewed'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="69-governed-region-study"','Fontes verificadas','Aguardam revisão','Retiradas do contexto automaticamente','Enviar para revisão','SEM INVENÇÃO'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('ESTUDO REGIONAL Fase 69: REPROVADO');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log(`ESTUDO REGIONAL Fase 69: aprovado — ${c.categories.length} categorias, fonte HTTPS, validade, revisão humana e sincronização segura.`);
