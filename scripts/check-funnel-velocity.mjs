import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/funnel-velocity.json")),engine=read(c.engine),api=read(c.api),page=read(c.page),fail=[];for(const m of["canonicalPipelineStage","cohortMoves","conversionRate","completedTransitions","medianDays","sampleSufficient","causalityClaimed:false","peopleRanking:false"])if(!engine.includes(m))fail.push(`motor incompleto: ${m}`);for(const m of["requireAccessContext","identity.supabase","pipeline_stage_moves","hierarchicalRls:true","Cache-Control","days*86_400_000"])if(!api.includes(m))fail.push(`API sem controle: ${m}`);for(const m of["FASE 40 · VELOCIDADE E CONVERSÃO","Ritmo real do funil","Passagem entre etapas","Tempo para sair de cada etapa","não atribui causalidade"])if(!page.includes(m))fail.push(`experiência incompleta: ${m}`);if(fail.length){console.error("VELOCIDADE DO FUNIL Fase 40: REPROVADO");fail.forEach(x=>console.error(`- ${x}`));process.exit(1)}console.log("VELOCIDADE DO FUNIL Fase 40: aprovado — coorte, conversão sequencial, duração e confiança explicável.");
