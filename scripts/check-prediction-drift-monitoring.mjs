import{readFileSync}from'node:fs';import{resolve}from'node:path';const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/prediction-drift-monitoring.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['prediction_drift_reports','generate_prediction_drift_report','probability_psi','population_shift','calibration_degradation','conversion_shift','baseline_start','current_sample','baseline_sample','automaticrollback','automaticcampaignchange','automaticpeopledecision'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);for(const marker of['baselineExplicit:true','minimumSampleRequired:true','humanReviewRequired:true','automaticRollback:false','automaticCampaignChange:false','automaticPeopleDecision:false'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="79-prediction-drift-monitoring"','MUDANÇA DE PÚBLICO','Saúde do modelo em produção','NENHUMA REAÇÃO AUTOMÁTICA','Amostra atual versus período anterior'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('DRIFT Fase 79: REPROVADA');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log(`DRIFT Fase 79: aprovado — PSI ${c.psi.watch}/${c.psi.alert}, Brier delta ${c.brierDelta.watch}/${c.brierDelta.alert} e revisão humana.`);
