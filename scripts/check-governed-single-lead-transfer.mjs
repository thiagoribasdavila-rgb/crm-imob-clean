import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/governed-single-lead-transfer.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),entry=read(c.entry),fail=[];for(const marker of ["transfer_single_lead","for update","is distinct from p_expected_owner_id","transfer_owner_conflict","transfer_direct_team_required","lead_transfer_batches","lead_transfer_items","update public.tasks","responsável transferido","singleownerpreserved"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of ["TRANSFER_FORBIDDEN","TRANSFER_LEAD_NOT_VISIBLE","expectedOwnerId","minimumReasonLength","transfer_single_lead","owner_conflict","409"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);if(!page.includes('data-phase="53-single-lead-transfer"')||!page.includes("Motivo obrigatório")||!page.includes("Nenhum histórico será apagado")||!entry.includes('/leads/${lead.id}/transfer'))fail.push("experiência de transferência incompleta");if(fail.length){console.error("TRANSFERÊNCIA INDIVIDUAL Fase 53: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log(`TRANSFERÊNCIA INDIVIDUAL Fase 53: aprovada — motivo ${c.minimumReasonLength}-${c.maximumReasonLength}, conflito otimista, histórico e responsável único.`);
