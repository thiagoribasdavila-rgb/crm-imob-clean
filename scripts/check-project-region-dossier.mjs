import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/project-region-dossier.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['refresh_project_intelligence_dossier','approve_project_intelligence_dossier','project_intelligence_events','verifiedessentialmaterials','humanapprovalrequired','personaldataused'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);for(const marker of['factsOnly:true','humanApprovalRequired:true','personalDataUsed:false','dossier.${action}'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="68-project-region-dossier"','Prontidão para IA','SOMENTE FATOS','Publicar para IA','Nunca convertidas em fatos'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('DOSSIÊ Fase 68: REPROVADO');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log('DOSSIÊ Fase 68: aprovado — evidências canônicas, lacunas explícitas, fontes, prontidão e publicação humana.');
