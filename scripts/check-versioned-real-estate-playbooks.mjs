import fs from"node:fs";const read=p=>fs.readFileSync(p,"utf8"),checks=[];function need(p,...terms){const s=read(p);for(const term of terms)checks.push([`${p}: ${term}`,s.includes(term)])}need("lib/ai/versioned-real-estate-playbooks.ts","PLAYBOOK_STAGES","abordagem","qualificacao","apresentacao","simulacao","visita","proposta","fechamento","reativacao","resolveActivePlaybook","playbookForPrompt");need("supabase/migrations/20260719103000_phase_85_versioned_real_estate_playbooks.sql","commercial_ai_playbooks","one_active","publish_commercial_ai_playbook","playbook_director_only","status='retired'","current_organization_id");need("app/api/ai/copilot/route.ts","resolveActivePlaybook","Playbook imobiliário ativo e aprovado","playbookForPrompt");need("app/api/v1/ai/playbooks/route.ts","directorPublishes:true","oneActivePerScope:true","developerOverride:true","humanApproval:true");need("app/(crm)/settings/ai-playbooks/page.tsx","85-versioned-real-estate-playbooks","8 ETAPAS COMERCIAIS","VERSÃO ATIVA ÚNICA","DIRETORIA APROVA");for(const[name,ok]of checks)console.log(`${ok?"✓":"✗"} ${name}`);if(checks.some(([,ok])=>!ok))process.exit(1);console.log(`\nFase 85 aprovada: ${checks.length} controles de playbooks imobiliários.`);
