import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/governed-absence-redistribution.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of["broker_absence_events","redistribute_absent_broker_leads","pg_advisory_xact_lock","for update skip locked","reports_to=manager_id","cp.availability='available'","90 seconds","project_distribution_members","lead_transfer_batches","lead_transfer_items","update public.tasks","managerownsleads","humanapproval","activeportfolioonly"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of['body.action === "cover_absence"',"reason.length < 10","p_ends_at","p_limit: limit","ABSENCE_REDISTRIBUTION_REJECTED","crm.distribution.absence_covered"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="55-absence-redistribution"',"Cobertura por ausência",'type="datetime-local"','minLength={10}',"Não é acionado por simples queda de conexão","window.confirm"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error("COBERTURA POR AUSÊNCIA Fase 55: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log(`COBERTURA POR AUSÊNCIA Fase 55: aprovada — decisão humana, até ${c.maximumBatchSize} leads e ${c.maximumDays} dias, mesma equipe e histórico preservado.`);
