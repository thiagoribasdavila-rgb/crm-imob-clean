import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/commercial-release-versioning.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),simulation=read(c.simulation),fail=[];for(const marker of["development_commercial_releases","commercial_release_events","commercial_release_one_active_idx","create_commercial_release_draft","activate_commercial_release","commercial_release_director_required","status='superseded'","price_table_material_id","payment_rule_id","commercial_release_id uuid references"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of["singleActiveRelease:true","directorActivation:true","create_commercial_release_draft","activate_commercial_release","commercial_release.activated"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="66-commercial-release-versioning"',"Pacote comercial vigente","Tabela de preço","Espelho de vendas","Condição de pagamento","Ativar versão"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);for(const marker of["development_commercial_releases","commercial_release_id","releaseSnapshot","commercialReleaseVersion"])if(!simulation.includes(marker))fail.push(`simulação sem vigência: ${marker}`);if(fail.length){console.error("VIGÊNCIA Fase 66: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log("VIGÊNCIA Fase 66: aprovada — pacote único ativo, fontes alinhadas, aprovação da diretoria e snapshot na simulação.");
