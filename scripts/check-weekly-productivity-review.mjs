import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/weekly-productivity-review.json")),api=read(c.api),page=read(c.page),fail=[];for(const source of ['from("tasks")','from("leads")','from("lead_visits")','from("activities")'])if(!api.includes(source))fail.push(`fonte ausente: ${source}`);for(const marker of ['eq("organization_id",org)','eq("assigned_to",actor)','eq("broker_id",actor)','eq("user_id",actor)','slice(0,5)','minimumSample:5','llmCost:0','peopleRanking:false'])if(!api.includes(marker))fail.push(`controle ausente: ${marker}`);if(!page.includes('data-phase="49-weekly-review"')||!page.includes("Amostra pequena")||!page.includes("Sem ranking de pessoas")||!page.includes("custo LLM zero"))fail.push("experiência semanal incompleta");if(fail.length){console.error("REVISÃO SEMANAL Fase 49: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log(`REVISÃO SEMANAL Fase 49: aprovada — ${c.periodDays} dias, até ${c.maximumPlanItems} focos e amostra mínima ${c.minimumSample}.`);
