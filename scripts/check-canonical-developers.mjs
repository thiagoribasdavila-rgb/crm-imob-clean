import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/canonical-developers.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),portfolio=read(c.portfolio),fail=[];for(const marker of["create table if not exists public.developers","developer_profile_events","developer_id uuid references public.developers","developers_org_cnpj_unique","update public.developments","upsert_developer_profile","developer_actor_forbidden","developer_cnpj_invalid","developer_website_invalid","sourceoftruth","legacynamecompatibility"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of["requireAccessContext","canManage","developer_id","upsert_developer_profile","developers.profile_saved","sourceOfTruth:true","legacyNameCompatibility:true"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="61-canonical-developers"',"FONTE ÚNICA","Razão social","Nome comercial","CNPJ","SLA padrão de comissão","Salvar incorporadora"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(!portfolio.includes('/developments/developers')||!portfolio.includes("Incorporadoras"))fail.push("entrada do portfólio ausente");if(fail.length){console.error("INCORPORADORAS Fase 61: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log(`INCORPORADORAS Fase 61: aprovada — entidade ${c.canonicalTable}, vínculo por ID, CNPJ único, auditoria e compatibilidade legada.`);
