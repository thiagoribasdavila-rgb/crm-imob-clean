import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/realtime-task-notifications.json")),sql=read(c.migration),page=read(c.page),topbar=read(c.topbar),fail=[];for(const m of["replica identity full","pg_publication_tables","supabase_realtime","add table public.task_reminders"])if(!sql.includes(m))fail.push(`realtime incompleto: ${m}`);for(const m of["FASE 45 · NOTIFICAÇÕES EM TEMPO REAL","postgres_changes","assigned_to=eq.","SUBSCRIBED","CHANNEL_ERROR","TIMED_OUT","removeChannel","aria-live","Atualizar"])if(!page.includes(m))fail.push(`página incompleta: ${m}`);for(const m of["reminderCount","task_reminders","postgres_changes","assigned_to=eq.","read_at","dismissed_at","removeChannel","99+"])if(!topbar.includes(m))fail.push(`topbar incompleto: ${m}`);if(fail.length){console.error("NOTIFICAÇÕES EM TEMPO REAL Fase 45: REPROVADO");fail.forEach(x=>console.error(`- ${x}`));process.exit(1)}console.log("NOTIFICAÇÕES EM TEMPO REAL Fase 45: aprovado — assinatura pessoal, contador real e fallback manual.");
