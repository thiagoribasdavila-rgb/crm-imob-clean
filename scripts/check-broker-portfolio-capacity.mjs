import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/broker-portfolio-capacity.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of["broker_capacity_limits","configure_broker_capacity","guard_broker_portfolio_capacity","leads_guard_broker_capacity","pg_advisory_xact_lock","broker_total_capacity_reached","broker_project_capacity_reached","currentactiveleads","currentlyoverlimit","peopleranking","reason text not null"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of['body.action === "configure_capacity"',"reason.length < 10","configure_broker_capacity","CAPACITY_UPDATE_REJECTED","crm.distribution.capacity_configured","capacityResult"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="56-broker-capacity"',"Limites de carteira por corretor","Máximo ativo","Máximo por projeto","Avisar em %",'minLength={10}',"Sem comparação pública"] )if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error("CAPACIDADE DE CARTEIRA Fase 56: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log(`CAPACIDADE DE CARTEIRA Fase 56: aprovada — teto total e por projeto, alerta ${c.ranges.warningPercent[0]}-${c.ranges.warningPercent[1]}%, bloqueio no banco e ranking zero.`);
