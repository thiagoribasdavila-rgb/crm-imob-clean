import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/controlled-team-transfer.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of["transfer_leads_to_team","pg_advisory_xact_lock","for update","team_transfer_manager_out_of_scope","reports_to=p_target_manager_id","project_distribution_members","count(l.id)::numeric/coalesce(m.weight,1)","lead_transfer_batches","lead_transfer_items","update public.tasks","managerownsleads","singlebrokerowner"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of['targetRole === "manager"',"transfer_leads_to_team","bulk_transfer_leads","reason.length < 10"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="54-team-transfer"',"O gerente não se torna responsável",'minLength={10}',"transferReason.trim().length < 10","payload.data?.teamTargetId"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error("TRANSFERÊNCIA ENTRE EQUIPES Fase 54: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log(`TRANSFERÊNCIA ENTRE EQUIPES Fase 54: aprovada — até ${c.maximumBatchSize} leads, gerente sem posse e equilíbrio ponderado por projeto.`);
