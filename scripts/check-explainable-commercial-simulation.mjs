import fs from"node:fs";const read=p=>fs.readFileSync(p,"utf8"),checks=[];function need(p,...terms){const s=read(p);for(const term of terms)checks.push([`${p}: ${term}`,s.includes(term)])}need("lib/commercial/explainable-payment-simulation.ts","rule_default","higher_down_payment","shorter_term","simulation_down_outside_rule","simulation_term_outside_rule","requiresHumanApproval:true","isProposal:false","creditApproved:false");need("supabase/migrations/20260719113000_phase_87_explainable_commercial_simulation.sql","scenario_type","calculation_version","assumptions","requires_human_approval","commercial_simulation_events","private.can_view_lead");need("app/api/v1/leads/[id]/commercial-simulation/route.ts","calculatePaymentScenario","activeReleaseOnly:true","activeRuleOnly:true","humanApprovalRequired:true","commercial_simulation_events");need("app/(crm)/leads/[id]/simulation/page.tsx","87-explainable-commercial-simulation","REGRA VIGENTE","CÁLCULO EXPLICÁVEL","NÃO É PROPOSTA","Comparação");for(const[name,ok]of checks)console.log(`${ok?"✓":"✗"} ${name}`);if(checks.some(([,ok])=>!ok))process.exit(1);console.log(`\nFase 87 aprovada: ${checks.length} controles de simulação comercial.`);
