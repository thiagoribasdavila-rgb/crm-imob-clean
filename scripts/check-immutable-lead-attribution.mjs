import{readFileSync}from'node:fs';import{resolve}from'node:path';const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/immutable-lead-attribution.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['lead_attribution_touches','first_attribution_id','last_attribution_id','record_lead_attribution_touch','capture_lead_attribution','firsttouchimmutable','duplicateprevented','backfill-first','pg_advisory_xact_lock'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);for(const marker of['firstTouchImmutable:true','lastTouchAppendOnly:true','noOverwrite:true','noPersonalData:true','hierarchicalRls:true'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="74-immutable-lead-attribution"','PRIMEIRA ORIGEM IMUTÁVEL','A primeira origem nunca é substituída','Registrar sem sobrescrever','Primeiro ao último toque'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('ATRIBUIÇÃO Fase 74: REPROVADA');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log(`ATRIBUIÇÃO Fase 74: aprovada — ${c.touchKinds.length} tipos, first-touch imutável, timeline, backfill e IDs de mídia.`);
