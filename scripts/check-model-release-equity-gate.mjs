import{readFileSync}from'node:fs';import{resolve}from'node:path';const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/model-release-equity-gate.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['model_release_gates','evaluate_model_release_gate','decide_model_release_gate','prohibited_attributes_found','segment_audit','segmentparityacceptable','explainabilityobserved','model_release_one_approved','model_gate_director_required','apply_active_conversion_calibration','g.status=\'approved\'','automaticrelease','automaticrollback'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);for(const marker of['protectedAttributesUsed:false','operationalSegmentsOnly:true','directorApprovalRequired:true','decisionReasonRequired:true','automaticRelease:false','automaticRollback:false','fallbackUntilApproved:true'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="80-model-release-equity-gate"','GATE FINAL DO MODELO','Liberar somente com evidência','ATRIBUTOS PROTEGIDOS PROIBIDOS','ROLLBACK HUMANO'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('GATE Fase 80: REPROVADO');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log(`GATE Fase 80: aprovado — ${c.minimumSegments} segmentos, scan de ${c.prohibitedAttributes.length} atributos, aprovação e rollback humanos.`);
