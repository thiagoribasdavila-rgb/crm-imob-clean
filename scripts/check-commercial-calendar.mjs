import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8");const contract=JSON.parse(read("config/commercial-calendar.json"));const page=read(contract.page),api=read(contract.api);const failures=[];for(const source of ['from("tasks")','from("lead_visits")','from("leads")'])if(!api.includes(source))failures.push(`fonte ausente: ${source}`);if(!api.includes("requireAccessContext")||!api.includes('eq("organization_id", organizationId)'))failures.push("API sem autenticação e organização");if(!api.includes("activeVisitKeys")||!api.includes("next_action_at"))failures.push("follow-up não é deduplicado da visita");if(!page.includes('data-phase="46-commercial-calendar"')||!page.includes("FASE 46 · AGENDA COMERCIAL UNIFICADA"))failures.push("tela não comprova a fase 46");for(const window of contract.windows)if(!page.includes(`'${window}'`)&&!page.includes(`"${window}"`))failures.push(`janela ausente: ${window}`);if(!page.includes("Atualizar")||!page.includes("removeChannel"))failures.push("fallback ou limpeza realtime ausente");if(!page.includes("nenhum cliente é contatado automaticamente"))failures.push("limite de automação não está explícito");if(failures.length){console.error("AGENDA COMERCIAL Fase 46: REPROVADA");for(const failure of failures)console.error(`- ${failure}`);process.exit(1)}console.log(`AGENDA COMERCIAL Fase 46: aprovada — ${contract.sources.length} fontes, ${contract.windows.length} períodos e deduplicação ativa.`);
