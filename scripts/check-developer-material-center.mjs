import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/developer-material-center.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),projectApi=read(c.projectApi),page=read(c.page),fail=[];for(const marker of["review_status text","project_material_review_events","review_project_material","material_review_actor_forbidden","material_review_not_current","currentversiononly","auditable"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of["coverageByDeveloper","coveragePercent","essentialAvailable","pendingReview","groupedByDeveloper:true","validityAware:true","review_project_material","materials.reviewed"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of["review_status","verified_at"])if(!projectApi.includes(marker))fail.push(`API do projeto incompleta: ${marker}`);for(const marker of['data-phase="67-developer-material-center"',"Cobertura por incorporadora","Pulso do portfólio","Validação pendente","reviewMaterial(material.id)","Publicar nova versão"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error("MATERIAIS Fase 67: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log("MATERIAIS Fase 67: aprovada — incorporadoras, cobertura, vigência, revisão, links privados e atualização simples.");
