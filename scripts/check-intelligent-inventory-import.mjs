import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/intelligent-inventory-import.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of["inventory_import_batches","inventory_import_rows","apply_inventory_import_batch","inventory_import_actor_forbidden","inventory_import_unresolved_rows","status='applying'","upsert_canonical_inventory_unit","humanapproved","atomic"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of["read-excel-file/node","parseCsv","fileSignature","crypto.subtle.digest","normalizeHeader","duplicate_unit","apply_inventory_import_batch","previewBeforeApply","humanApproval"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="65-intelligent-inventory-import"',"Importar tabela","Prévia obrigatória","Conflitos","Aplicar ao espelho",".xlsx ou .csv"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error("IMPORTAÇÃO Fase 65: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log("IMPORTAÇÃO Fase 65: aprovada — XLSX/CSV, reconciliação, prévia, conflitos, aprovação humana e aplicação atômica.");
