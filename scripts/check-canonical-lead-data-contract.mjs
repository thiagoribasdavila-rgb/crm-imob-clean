import{readFileSync}from'node:fs';import{resolve}from'node:path';const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/canonical-lead-data-contract.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['email_normalized','phone_normalized','source_normalized','identity_key','canonical_contract_version','data_quality_percent','data_quality_issues','apply_canonical_lead_contract','refresh_canonical_lead_quality','llmcost','personaldataexposed'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);const total=Object.values(c.dimensions).reduce((a,b)=>a+b,0);if(total!==100)fail.push(`pesos somam ${total}`);for(const marker of['hierarchicalVisibility:true','sensitiveFieldsReturned:false','scorePredictionSeparated:true','llmCost:0'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="71-canonical-lead-contract"','Qualidade confiável antes da predição','Este percentual avalia dados; não é probabilidade de compra','Fila de saneamento','CUSTO IA ZERO'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('CONTRATO DA LEAD Fase 71: REPROVADO');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log(`CONTRATO DA LEAD Fase 71: aprovado — ${Object.keys(c.dimensions).length} dimensões, 100 pontos, identidade normalizada e custo LLM zero.`);
