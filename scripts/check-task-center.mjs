import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/task-center.json")),engine=read(c.engine),api=read(c.api),page=read(c.page),fail=[];for(const m of["priorityScore","byOwner","withoutDueDate","unassigned","hierarchicalRls:true","automaticAssignment:false","peopleRanking:false"])if(!engine.includes(m))fail.push(`motor incompleto: ${m}`);for(const m of["requireAccessContext","identity.supabase","complete","postpone_one_day","TASK_ACTION_INVALID","organization_id","Cache-Control"])if(!api.includes(m))fail.push(`API sem controle: ${m}`);if(!["FASE 41 · CENTRAL DE TAREFAS","FASE 42 · CRIAÇÃO RÁPIDA","FASE 43 · TAREFAS RECORRENTES"].some(marker=>page.includes(marker)))fail.push("experiência incompleta: selo evolutivo da central");for(const m of["Minha fila","Equipe visível","Sem prazo","/api/v1/tasks"])if(!page.includes(m))fail.push(`experiência incompleta: ${m}`);if(page.includes('.from("tasks").update'))fail.push("página ainda altera tarefa diretamente");if(fail.length){console.error("CENTRAL DE TAREFAS Fase 41: REPROVADO");fail.forEach(x=>console.error(`- ${x}`));process.exit(1)}console.log("CENTRAL DE TAREFAS Fase 41: aprovado — prioridade, responsáveis, prazos e ações governadas.");
