import{readFileSync}from'node:fs';import{resolve}from'node:path';const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/calibrated-predictive-score.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['conversion_calibration_models','build_conversion_calibration_candidate','activate_conversion_calibration','apply_active_conversion_calibration','expected_calibration_error','brier_score','reliability_bins','conversion_calibration_one_active','calibration_director_required','automaticpromotion'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);for(const marker of['commercialScoreSeparated:true','probabilityCalibrated:true','lowSampleFallback:true','humanReviewRequired:true','directorApprovalRequired:true','automaticPromotion:false','peopleRanking:false'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="77-calibrated-predictive-score"','SCORE NÃO É PROBABILIDADE','Previsão calibrada com resultados reais','ATIVAÇÃO EXCLUSIVA DA DIRETORIA','Amostra insuficiente mantém o modelo atual'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('CALIBRAÇÃO Fase 77: REPROVADA');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log(`CALIBRAÇÃO Fase 77: aprovada — ${c.method}, Brier/ECE, fallback e promoção humana exclusiva.`);
