import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8");const c=JSON.parse(read("config/pipeline-aging.json")),engine=read(c.engine),api=read(c.api),page=read(c.page),fail=[];for(const m of["canonicalPipelineStage","StageMove","ageDays","stalledRate","movementCount30d","estimatedEntry","automaticTransfer:false","velocityClaimed"])if(!engine.includes(m))fail.push(`motor incompleto: ${m}`);for(const m of["requireAccessContext","identity.supabase","pipeline_stage_moves","hierarchicalRls:true","Cache-Control"])if(!api.includes(m))fail.push(`API sem controle: ${m}`);for(const m of["FASE 39 · AGING DO PIPELINE","Onde o pipeline está parando","Fila prioritária","Cobertura do horário real","nenhuma transferência é automática"])if(!page.includes(m))fail.push(`experiência incompleta: ${m}`);if(fail.length){console.error("AGING DO PIPELINE Fase 39: REPROVADO");fail.forEach(x=>console.error(`- ${x}`));process.exit(1)}console.log("AGING DO PIPELINE Fase 39: aprovado — tempo por etapa, gargalos, cobertura e fila de ação governada.");
