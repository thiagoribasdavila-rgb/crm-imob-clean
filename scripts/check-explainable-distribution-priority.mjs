import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/explainable-distribution-priority.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of["lead_distribution_priority_rules","configure_distribution_priority","distribute_project_leads_v3","sla_source_priority_v3","sla_minutes","priority between 1 and 10","pg_advisory_xact_lock","broker_capacity_limits","distribution_no_broker_with_capacity","personaldataused","peopleranking","for update of l skip locked"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of['body.action === "configure_priority"',"configure_distribution_priority","PRIORITY_UPDATE_REJECTED","crm.distribution.priority_configured","priorityResult","leadSources"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);if(!api.includes("distribute_project_leads_v3")&&!api.includes("distribute_project_leads_v4"))fail.push("API incompleta: prioridade não conectada ao motor atual");for(const marker of['data-phase="57-distribution-priority"',"Prioridade por SLA e origem","Nome, renda, gênero, idade",'minLength={10}',"maior pressão de SLA","Salvar prioridade"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error("PRIORIDADE DE DISTRIBUIÇÃO Fase 57: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log(`PRIORIDADE DE DISTRIBUIÇÃO Fase 57: aprovada — ${c.algorithm}, SLA ${c.slaMinutesRange[0]}-${c.slaMinutesRange[1]} min, dados pessoais zero e capacidade respeitada.`);
