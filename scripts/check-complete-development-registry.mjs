import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/complete-development-registry.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),portfolio=read(c.portfolio),fail=[];for(const marker of["project_code text","address_line text","typologies text[]","sales_cycle_status text","development_profile_events","upsert_complete_development","development_actor_forbidden","development_developer_invalid","development_price_range_invalid","development_sales_dates_invalid","developer_name=developer.trade_name"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of["requireAccessContext","upsert_complete_development","canManage","completeRegistry","developments.profile_saved"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="62-complete-development-registry"',"Cadastro completo","Incorporadora canônica","Localização","Produto e tipologias","Ciclo comercial","Salvar empreendimento"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(!portfolio.includes('/developments/registry')||!portfolio.includes("Novo empreendimento"))fail.push("entrada do cadastro ausente");if(fail.length){console.error("EMPREENDIMENTOS Fase 62: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log("EMPREENDIMENTOS Fase 62: aprovada — incorporadora canônica, localização, produto, faixas, datas, ciclo e auditoria.");
