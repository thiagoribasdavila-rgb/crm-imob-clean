import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/productivity-block-closure.json")),pkg=JSON.parse(read("package.json")),homologation=read("app/(crm)/atlas-v3/homologation/page.tsx"),fail=[];if(c.range[1]-c.range[0]+1!==c.contracts.length)fail.push("fases e contratos não reconciliam");for(const contract of c.contracts)if(!existsSync(resolve(process.cwd(),contract)))fail.push(`contrato ausente: ${contract}`);for(const gate of c.gates)if(!pkg.scripts[gate])fail.push(`gate ausente: ${gate}`);for(const marker of ['data-phase="50-productivity-closure"','ENGENHARIA LOCAL','EVIDÊNCIA REAL PENDENTE','quatro perfis','dois tenants','Mobile','Realtime','OAuth'])if(!homologation.includes(marker))fail.push(`painel incompleto: ${marker}`);if(c.localEngineeringPercent!==100||!c.operationalPercentRequiresEvidence)fail.push("percentuais misturam engenharia e operação");if(c.performanceLimits.dailySteps>7||c.performanceLimits.weeklyPlan>5||c.performanceLimits.taskReadLimit>2000||c.performanceLimits.calendarReadPerSource>2000)fail.push("limite de desempenho excedido");if(fail.length){console.error("PRODUTIVIDADE Fase 50: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log(`PRODUTIVIDADE Fase 50: aprovada — ${c.contracts.length} fases reconciliadas, ${c.gates.length} gates e ${c.externalEvidence.length} evidências externas explícitas.`);
