import fs from"node:fs";const read=p=>fs.readFileSync(p,"utf8"),checks=[];function need(p,...terms){const s=read(p);for(const term of terms)checks.push([`${p}: ${term}`,s.includes(term)])}need("lib/ai/conversational-qualification.ts","QUALIFICATION_FIELDS","qualificationProgress","nextQuestion","readyForPresentation");need("supabase/migrations/20260719110000_phase_86_structured_conversational_qualification.sql","lead_qualification_profiles","lead_qualification_events","record_structured_qualification","qualification_owner_only","sync_qualification_owner","rawConversationStored","private.can_view_lead","qualificationAnswers");need("app/api/v1/leads/[id]/conversational-qualification/route.ts","structuredOnly:true","oneQuestionAtATime:true","brokerConfirmation:true","rawConversationStored:false","noAutomaticInference:true");need("app/api/ai/copilot/route.ts","lead_qualification_profiles","qualificationProgress","brokerConfirmed: true");need("app/(crm)/leads/[id]/qualification/page.tsx","86-structured-conversational-qualification","UMA PERGUNTA POR VEZ","CORRETOR CONFIRMA","CONVERSA BRUTA: ZERO");for(const[name,ok]of checks)console.log(`${ok?"✓":"✗"} ${name}`);if(checks.some(([,ok])=>!ok))process.exit(1);console.log(`\nFase 86 aprovada: ${checks.length} controles de qualificação conversacional.`);
