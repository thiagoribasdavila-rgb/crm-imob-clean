import{readFileSync}from'node:fs';import{resolve}from'node:path';const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/operational-prediction-explainability.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['lead_prediction_explanations','build_lead_prediction_explanation','raw_probability','calibrated_probability','positive_factor_keys','risk_factor_keys','missing_signal_keys','recommended_action_key','apply_active_conversion_calibration','with recursive descendants','reports_to','expires_at','llmcost'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);for(const marker of['scoreProbabilitySeparated:true','calibratedWhenApproved:true','confidenceExplicit:true','structuredExplanation:true','zeroLlmCost:true','humanReviewRequired:true','automaticDecision:false','peopleRanking:false'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="78-operational-prediction-explainability"','ENTENDA O PORQUÊ','Probabilidade explicada para agir melhor','PRÓXIMA MELHOR AÇÃO','CUSTO IA ZERO'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('EXPLICABILIDADE Fase 78: REPROVADA');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log(`EXPLICABILIDADE Fase 78: aprovada — ${c.factorKeys.length} fatores, ${c.actionKeys.length} ações, hierarquia, confiança e zero LLM.`);
