import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/recurring-tasks.json")),sql=read(c.migration),api=read(c.api),worker=read(c.worker),page=read(c.page),fail=[];for(const m of["task_recurrences","max_occurrences between 2 and 100","ends_at>=next_run_at","tasks_recurrence_occurrence_unique","create_recurring_task","process_due_task_recurrences","for update skip locked","active=not","service_role"])if(!sql.includes(m))fail.push(`banco incompleto: ${m}`);for(const m of["TASK_RECURRENCE_INVALID","create_recurring_task","p_assigned_to","p_lead_id","maxOccurrences < 2","maxOccurrences > 100","cancel_recurrence","active: false"])if(!api.includes(m))fail.push(`API incompleta: ${m}`);for(const m of["ATLAS_CRON_SECRET","process_due_task_recurrences","logger.info","logger.error"])if(!worker.includes(m))fail.push(`worker incompleto: ${m}`);for(const m of["FASE 43 · TAREFAS RECORRENTES","Repetir tarefa","daily","weekly","monthly","endsAt","maxOccurrences","Encerrar repetição"])if(!page.includes(m))fail.push(`experiência incompleta: ${m}`);if(fail.length){console.error("TAREFAS RECORRENTES Fase 43: REPROVADO");fail.forEach(x=>console.error(`- ${x}`));process.exit(1)}console.log("TAREFAS RECORRENTES Fase 43: aprovado — término, limite, atomicidade e worker sem duplicação.");
