import{readFileSync}from"node:fs";import{resolve}from"node:path";const read=p=>readFileSync(resolve(process.cwd(),p),"utf8"),c=JSON.parse(read("config/canonical-sales-mirror.json")),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of["typology_id uuid references","canonical_unit_key","properties_org_canonical_unit_unique","inventory_unit_events","upsert_canonical_inventory_unit","inventory_actor_forbidden","inventory_concurrent_update","inventory_active_reservation","inventory_version","availability_updated_at","optimisticconcurrency","reservationguard"])if(!sql.includes(marker))fail.push(`banco incompleto: ${marker}`);for(const marker of["upsert_canonical_inventory_unit","expectedUpdatedAt","inventory_unit_events","canonicalMirror","reservationGuard","inventory_updated"])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="64-canonical-sales-mirror"',"ESPELHO CANÔNICO","Cadastrar unidade","Tipologia canônica","Motivo da alteração","inventory_version","availability_updated_at"])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error("ESPELHO Fase 64: REPROVADA");for(const f of fail)console.error(`- ${f}`);process.exit(1)}console.log("ESPELHO Fase 64: aprovado — unidade única, tipologia canônica, concorrência, reserva e histórico de preço/status.");
