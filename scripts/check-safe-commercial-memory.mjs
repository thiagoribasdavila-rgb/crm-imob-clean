import fs from"node:fs";const read=p=>fs.readFileSync(p,"utf8"),checks=[];function need(p,...terms){const value=read(p);for(const term of terms)checks.push([`${p}: ${term}`,value.includes(term)])}need("supabase/migrations/20260719093000_phase_83_safe_commercial_memory.sql","lead_commercial_memory_states","unique(organization_id,lead_id)","record_structured_copilot_memory","legacyEntriesPurged","sync_structured_memory_owner","180 days","private.can_view_lead");need("app/api/ai/copilot/route.ts","record_structured_copilot_memory","rawConversationStored: false","structuredMemoryFromGovernedContext");checks.push(["Copiloto não grava pergunta/resposta bruta",!read("app/api/ai/copilot/route.ts").includes("append_lead_copilot_interaction")]);need("app/api/v1/leads/[id]/commercial-memory/route.ts","structuredOnly:true","rawConversationStored:false","retentionDays:180","reset_structured_copilot_memory");need("app/(crm)/leads/[id]/memory/page.tsx","83-safe-commercial-memory","MEMÓRIA SEM CONVERSA BRUTA","NUNCA MISTURA CLIENTES","RETENÇÃO 180 DIAS");for(const[name,ok]of checks)console.log(`${ok?"✓":"✗"} ${name}`);if(checks.some(([,ok])=>!ok))process.exit(1);console.log(`\nFase 83 aprovada: ${checks.length} controles de memória comercial segura.`);
