import{readFileSync}from'node:fs';import{resolve}from'node:path';const read=p=>readFileSync(resolve(process.cwd(),p),'utf8'),c=JSON.parse(read('config/unique-lead-identity.json')),sql=read(c.migration).toLowerCase(),api=read(c.api),page=read(c.page),fail=[];for(const marker of['canonical_lead_id','duplicate_state','lead_identity_merge_events','merge_duplicate_lead_identity','pg_advisory_xact_lock','singleactiverecord','historypreserved','ownershippreservedinsnapshot','automaticmerge'])if(!sql.replaceAll('_','').includes(marker.replaceAll('_','')))fail.push(`banco incompleto: ${marker}`);for(const marker of['identityFingerprint','recommendedMasterId','maskedIdentity:true','humanDecisionRequired:true','automaticMerge:false','historyPreserved:true','ownerSnapshot:true'])if(!api.includes(marker))fail.push(`API incompleta: ${marker}`);for(const marker of['data-phase="72-unique-lead-identity"','Uma pessoa, uma lead ativa','HISTÓRICO PRESERVADO','Nada é unido automaticamente','Manter esta como principal'])if(!page.includes(marker))fail.push(`experiência incompleta: ${marker}`);if(fail.length){console.error('IDENTIDADE Fase 72: REPROVADA');fail.forEach(item=>console.error(`- ${item}`));process.exit(1)}console.log('IDENTIDADE Fase 72: aprovada — match exato, contato mascarado, decisão humana, registro principal e histórico preservado.');
