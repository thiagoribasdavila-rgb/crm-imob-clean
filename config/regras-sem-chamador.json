{
  "$comentario": "Regras com contrato e SEM chamador de produção. O portão falha quando esta lista CRESCE — é o momento em que a quinta ocorrência de 'fio no chão' nasceria. Ao ligar o fio, remova a linha daqui.",
  "$correcao": "2026-08-04: `lib/ai/quem-espera-uma-pessoa.ts` saiu da lista. Ela nunca deveria ter entrado — DUAS rotas de produção já importavam `AGENTES_QUE_ESPERAM_PESSOA`, mas o detector só reconhecia export de FUNÇÃO (`= (` ou `= async`); uma constante de dados (`export const X = [...] as const`) não casava, e o módulo foi acusado de não ter chamador. Portão que acusa quem está certo é portão que ninguém lê depois — corrigido para reconhecer `export const`/`export let` por atribuição, não só por chamada de função.",
  "congeladoEm": "2026-08-03",
  "regras": [
    "lib/atlas/prontidao-do-catalogo.ts",
    "lib/crm/traduz-status-de-planilha.ts",
    "lib/deploy/autorizacao-de-instalacao.ts",
    "lib/whatsapp/pool-de-numeros.ts"
  ]
}
