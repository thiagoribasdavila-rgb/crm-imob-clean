import{type NextRequest}from"next/server";import{apiError,apiSuccess,structuredApiLog}from"@/lib/api/core";import{enforceRateLimit,requireAccessContext}from"@/lib/api/security";import{getSupabaseAdmin}from"@/lib/supabase/admin";import{options,qualificationProgress,type QualificationProfile}from"@/lib/ai/conversational-qualification";
const shape=(row:Record<string,unknown>|null):QualificationProfile=>row?{purpose:String(row.purpose_key||""),timeline:String(row.timeline_key||""),financing:String(row.financing_key||""),budget_readiness:String(row.budget_readiness_key||""),region_readiness:String(row.region_readiness_key||""),unit_profile:String(row.unit_profile_key||""),decision_role:String(row.decision_role_key||""),contact_preference:String(row.contact_preference_key||"")}:{};
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){const rate=enforceRateLimit(request,{limit:60,scope:"lead.qualification.read"});if(!rate.ok)return rate.response;const access=await requireAccessContext(request);if(!access.ok)return access.response;const{id}=await params,{data:lead}=await access.supabase.from("leads").select("id,name,assigned_to").eq("id",id).maybeSingle();if(!lead)return apiError("LEAD_NOT_FOUND","Lead fora do seu escopo.",access.meta,{status:404});const[{data,error},{data:events}]=await Promise.all([access.supabase.from("lead_qualification_profiles").select("version,purpose_key,timeline_key,financing_key,budget_readiness_key,region_readiness_key,unit_profile_key,decision_role_key,contact_preference_key,answered_count,completed_at,updated_at").eq("lead_id",id).maybeSingle(),access.supabase.from("lead_qualification_events").select("id,changed_fields,source,created_at").eq("lead_id",id).order("created_at",{ascending:false}).limit(30)]);if(error)return apiError("QUALIFICATION_UNAVAILABLE","Aplique a migration da Fase 86.",access.meta,{status:503});const profile=shape(data as Record<string,unknown>|null);return apiSuccess({lead,profile,progress:qualificationProgress(profile),events:events??[],options,canAnswer:lead.assigned_to===access.access.profile.id,policy:{structuredOnly:true,oneQuestionAtATime:true,brokerConfirmation:true,rawConversationStored:false,noAutomaticInference:true,exclusiveLead:true,exclusiveBroker:true}},access.meta,{headers:rate.headers})}
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const rate=enforceRateLimit(request,{limit:30,scope:"lead.qualification.answer"});if(!rate.ok)return rate.response;const access=await requireAccessContext(request);if(!access.ok)return access.response;const{id}=await params,body=await request.json().catch(()=>null)as{field?:string;value?:string}|null,field=String(body?.field||""),value=String(body?.value||"");if(!(field in options)||!options[field as keyof typeof options].includes(value))return apiError("QUALIFICATION_INVALID","Selecione uma resposta comercial válida.",access.meta,{status:400});const{data,error}=await getSupabaseAdmin().rpc("record_structured_qualification",{p_actor_id:access.access.profile.id,p_organization_id:access.access.organization.id,p_lead_id:id,p_field:field,p_value:value});if(error)return apiError("QUALIFICATION_REJECTED","Somente o corretor responsável pode confirmar esta resposta.",access.meta,{status:403});structuredApiLog("info","lead.structured_qualification_recorded",request,access.meta,{leadId:id,field});return apiSuccess(data,access.meta,{status:201,headers:rate.headers})}
