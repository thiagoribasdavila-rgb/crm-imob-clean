import{type NextRequest}from"next/server";import{apiError,apiSuccess,structuredApiLog}from"@/lib/api/core";import{enforceRateLimit,requireAccessContext}from"@/lib/api/security";import{getSupabaseAdmin}from"@/lib/supabase/admin";export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){const rate=enforceRateLimit(request,{limit:60,scope:'lead.contact-preferences.read'});if(!rate.ok)return rate.response;const access=await requireAccessContext(request);if(!access.ok)return access.response;const{id}=await params,{data:lead}=await access.supabase.from('leads').select('id,name').eq('id',id).maybeSingle();if(!lead)return apiError('LEAD_NOT_FOUND','Lead fora do seu escopo.',access.meta,{status:404});const db=getSupabaseAdmin(),{data,error}=await db.from('lead_contact_preferences').select('id,channel,consent_status,lawful_basis,evidence,preferred,allowed_start,allowed_end,allowed_days,timezone,valid_until,granted_at,revoked_at,updated_at').eq('organization_id',access.access.organization.id).eq('lead_id',id).order('channel');if(error)return apiError('CONTACT_PREFERENCES_UNAVAILABLE','Aplique a migration da Fase 73.',access.meta,{status:503});const channels=['whatsapp','email','phone','sms'],preferences=channels.map(channel=>data?.find(item=>item.channel===channel)??{channel,consent_status:'unknown',preferred:false,allowed_start:'09:00',allowed_end:'20:00',allowed_days:[1,2,3,4,5,6],timezone:'America/Sao_Paulo'}),eligibility=Object.fromEntries(await Promise.all(channels.map(async channel=>{const{data:result}=await db.rpc('check_lead_contact_eligibility',{p_organization_id:access.access.organization.id,p_lead_id:id,p_channel:channel});return[channel,result]})));return apiSuccess({lead,preferences,eligibility,policy:{singleSourceOfTruth:true,optOutImmediate:true,automationRecheck:true,channelSpecific:true,timeWindowAware:true,humanEvidenceRequired:true}},access.meta,{headers:rate.headers})}export async function PUT(request:NextRequest,{params}:{params:Promise<{id:string}>}){const rate=enforceRateLimit(request,{limit:20,scope:'lead.contact-preferences.write'});if(!rate.ok)return rate.response;const access=await requireAccessContext(request);if(!access.ok)return access.response;const{id}=await params,body=await request.json().catch(()=>null)as Record<string,unknown>|null;if(!body)return apiError('CONTACT_PREFERENCE_INVALID','Preferência não informada.',access.meta,{status:400});const{data,error}=await getSupabaseAdmin().rpc('set_lead_contact_preference',{p_actor_id:access.access.profile.id,p_organization_id:access.access.organization.id,p_lead_id:id,p_channel:body.channel,p_status:body.status,p_lawful_basis:body.lawfulBasis||'',p_evidence:body.evidence||'',p_preferred:Boolean(body.preferred),p_allowed_start:body.allowedStart||'09:00',p_allowed_end:body.allowedEnd||'20:00',p_allowed_days:body.allowedDays||[1,2,3,4,5,6],p_timezone:body.timezone||'America/Sao_Paulo',p_valid_until:body.validUntil||null});if(error)return apiError('CONTACT_PREFERENCE_REJECTED',error.message,access.meta,{status:409});structuredApiLog('info','lead.contact_preference_updated',request,access.meta,{leadId:id,channel:body.channel,status:body.status});return apiSuccess(data,access.meta,{headers:rate.headers})}
