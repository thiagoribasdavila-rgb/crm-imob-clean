import{type NextRequest}from"next/server";import{apiError,apiSuccess}from"@/lib/api/core";import{enforceRateLimit,requireAccessContext}from"@/lib/api/security";import{evaluateReactivationPlan,type Quality}from"@/lib/commercial/consented-reactivation-policy";
export async function GET(request:NextRequest){const rate=enforceRateLimit(request,{limit:30,scope:"reactivation.governance.read"});if(!rate.ok)return rate.response;const access=await requireAccessContext(request);if(!access.ok)return access.response;const{data,error}=await access.supabase.from("lead_reactivation_batches").select("id,name,owner_id,status,source_type,consent_basis,quality_status,daily_cap,interval_seconds,send_window_start,send_window_end,maximum_attempts,cooling_off_hours,stop_on_reply,stop_on_opt_out,official_api_only,imported_count,eligible_count,queued_count,delivered_count,read_count,replied_count,failed_count,paused_reason,created_at").order("created_at",{ascending:false}).limit(100);if(error)return apiError("REACTIVATION_GOVERNANCE_UNAVAILABLE","Aplique a migration da Fase 89.",access.meta,{status:503});const batches=(data??[]).map(b=>({...b,deliveryRate:b.queued_count?Math.round(b.delivered_count/b.queued_count*100):0,replyRate:b.delivered_count?Math.round(b.replied_count/b.delivered_count*100):0,stopRequired:b.quality_status==="red"||b.failed_count>Math.max(5,b.queued_count*.1)}));return apiSuccess({batches,summary:{batches:batches.length,running:batches.filter(b=>["queued","running"].includes(b.status)).length,paused:batches.filter(b=>Boolean(b.paused_reason)||b.quality_status==="red").length,replies:batches.reduce((n,b)=>n+b.replied_count,0)},policy:{officialApiOnly:true,approvedTemplateOnly:true,documentedConsent:true,stopOnReply:true,stopOnOptOut:true,stopOnInvalidPhone:true,maximumAttempts:2,coolingOffHours:72,brokerChoiceOnBadExperience:true,humanApproval:true}},access.meta,{headers:{...rate.headers,"Cache-Control":"no-store"}})}
export async function POST(request:NextRequest){const rate=enforceRateLimit(request,{limit:20,scope:"reactivation.governance.simulate"});if(!rate.ok)return rate.response;const access=await requireAccessContext(request);if(!access.ok)return access.response;const body=await request.json().catch(()=>null)as{quality?:Quality;dailyCap?:number;intervalSeconds?:number;consent?:boolean;template?:boolean;contacts?:number;officialApi?:boolean}|null;if(!body)return apiError("PLAN_INVALID","Informe o plano.",access.meta,{status:400});const plan=evaluateReactivationPlan({quality:body.quality||"unknown",requestedDailyCap:Number(body.dailyCap||100),requestedIntervalSeconds:Number(body.intervalSeconds||30),consentBasis:body.consent?"simulated-consent":null,approvedTemplate:Boolean(body.template),eligibleContacts:Number(body.contacts||0),officialApiReady:Boolean(body.officialApi)});return apiSuccess({plan,simulationOnly:true,noMessagesCreated:true,noExternalAction:true},access.meta,{headers:rate.headers})}
