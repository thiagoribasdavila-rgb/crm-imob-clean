"use client";
import Link from"next/link";import{useCallback,useEffect,useState}from"react";import{supabase}from"@/lib/supabase";import{AtlasBadge,AtlasEmpty,AtlasSkeleton}from"@/components/ui/AtlasUI";import{AtlasCard,AtlasCardHeader,AtlasMetric}from"@/components/ui/AtlasCard";
type Reminder={id:string;kind:"upcoming"|"overdue";task_due_at:string;priority:string;created_at:string;read_at:string|null;task:{id:string;title:string;due_at:string;status:string;lead_id:string|null}|Array<{id:string;title:string;due_at:string;status:string;lead_id:string|null}>};type Data={summary:{open:number;unread:number;overdue:number};reminders:Reminder[];governance:{customerContacted:false;automaticMessage:false;deduplicated:true}};const label=(value:string)=>new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(value));
export default function NotificationsPage(){const[data,setData]=useState<Data|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState("");const token=useCallback(async()=>{const{data:session}=await supabase.auth.getSession();return session.session?.access_token||""},[]);const load=useCallback(async()=>{setLoading(true);try{const response=await fetch("/api/v1/task-reminders",{headers:{Authorization:`Bearer ${await token()}`},cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error();setData(body.data||body)}catch{setError("Não foi possível carregar seus lembretes.")}finally{setLoading(false)}},[token]);useEffect(()=>{void load()},[load]);async function act(id:string,action:"read"|"dismiss"){const response=await fetch("/api/v1/task-reminders",{method:"PATCH",headers:{"Content-Type":"application/json",Authorization:`Bearer ${await token()}`},body:JSON.stringify({id,action})});if(!response.ok)setError("Não foi possível atualizar o lembrete.");else await load()}
return <div className="space-y-6 pb-10" data-phase="44-smart-reminders"><header><AtlasBadge tone="warning">FASE 44 · LEMBRETES INTELIGENTES</AtlasBadge><h1 className="mt-4 text-3xl font-semibold tracking-[-.04em] text-white">Antecipe o que precisa de ação</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">Alertas pessoais por prazo e prioridade, sem mensagens automáticas ao cliente e sem repetição desnecessária.</p></header>{error?<div className="rounded-2xl border border-rose-400/20 bg-rose-400/10 p-4 text-sm text-rose-200">{error}</div>:null}<section className="grid gap-4 sm:grid-cols-3"><AtlasMetric label="Lembretes abertos" value={loading?"—":data?.summary.open??0} detail="Sua caixa operacional" trend="ATIVOS" tone="blue"/><AtlasMetric label="Não lidos" value={loading?"—":data?.summary.unread??0} detail="Revisar agora" trend="NOVOS" tone="violet"/><AtlasMetric label="Tarefas vencidas" value={loading?"—":data?.summary.overdue??0} detail="Maior urgência" trend="SLA" tone="rose"/></section><AtlasCard><AtlasCardHeader eyebrow="Caixa pessoal" title="Ações no momento certo" description="Alta prioridade avisa antes; itens normais e baixos usam janelas menores para reduzir ruído." action={<button onClick={()=>void load()} className="atlas-button-secondary">Atualizar</button>}/><div className="grid gap-3 p-5 sm:p-6">{loading?[1,2,3].map(item=><AtlasSkeleton key={item} className="h-36"/>):data?.reminders.length?data.reminders.map(reminder=>{const task=Array.isArray(reminder.task)?reminder.task[0]:reminder.task;return <article key={reminder.id} className={`rounded-2xl border p-4 ${reminder.kind==="overdue"?"border-rose-400/20 bg-rose-400/[.04]":"border-amber-400/15 bg-amber-400/[.03]"}`}><div className="flex items-start justify-between gap-3"><div><strong className="text-sm text-white">{task?.title||"Tarefa comercial"}</strong><p className="mt-1 text-xs text-slate-500">Prazo {label(reminder.task_due_at)} · prioridade {reminder.priority}</p></div><AtlasBadge tone={reminder.kind==="overdue"?"danger":"warning"}>{reminder.kind==="overdue"?"VENCIDA":"PRÓXIMA"}</AtlasBadge></div><div className="mt-4 flex flex-wrap gap-2">{task?.lead_id?<Link href={`/leads/${task.lead_id}`} className="atlas-button-primary">Abrir lead</Link>:<Link href="/tasks" className="atlas-button-primary">Abrir tarefa</Link>}{!reminder.read_at?<button onClick={()=>void act(reminder.id,"read")} className="atlas-button-secondary">Marcar como lido</button>:null}<button onClick={()=>void act(reminder.id,"dismiss")} className="atlas-button-secondary">Dispensar</button></div></article>}):<AtlasEmpty title="Nenhum lembrete pendente" description="Sua operação está dentro das janelas configuradas."/>}</div></AtlasCard><p className="rounded-2xl border border-cyan-400/15 bg-cyan-400/[.05] p-4 text-xs leading-5 text-slate-400">Os lembretes são internos e deduplicados pelo prazo da tarefa. Nenhum cliente é contatado e nenhuma tarefa é concluída automaticamente.</p></div>}
