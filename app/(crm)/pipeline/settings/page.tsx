"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";
import type { PipelineStageDefinition } from "@/lib/atlas/pipeline-stages";
import { AtlasBadge, AtlasSkeleton } from "@/components/ui/AtlasUI";
import { AtlasCard, AtlasCardHeader } from "@/components/ui/AtlasCard";

export default function PipelineSettingsPage() {
  const [stages, setStages] = useState<PipelineStageDefinition[]>([]); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [message, setMessage] = useState("");
  async function request(method = "GET", body?: unknown) { const { data } = await supabase.auth.getSession(); if (!data.session?.access_token) throw new Error("Sessão expirada."); const response = await fetch("/api/v1/pipeline/stages", { method, headers: { Authorization: `Bearer ${data.session.access_token}`, "Content-Type": "application/json" }, body: body ? JSON.stringify(body) : undefined }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || "Falha ao configurar etapas."); return payload; }
  useEffect(() => { void request().then((payload) => setStages(payload.stages)).catch((error) => setMessage(error.message)).finally(() => setLoading(false)); }, []);
  async function save() { setSaving(true); setMessage(""); try { const payload = await request("PUT", { stages }); setStages(payload.stages); setMessage("Etapas salvas. O Kanban e o forecast já usam a nova configuração."); } catch (error) { setMessage(error instanceof Error ? error.message : "Falha ao salvar."); } finally { setSaving(false); } }
  if (loading) return <AtlasSkeleton className="h-96 w-full" />;
  return <div className="mx-auto max-w-5xl space-y-6 pb-12" data-phase="31-stage-settings"><div><Link href="/pipeline" className="text-xs font-semibold text-sky-300">← Voltar ao pipeline</Link><h1 className="mt-4 text-3xl font-semibold text-white">Etapas do funil</h1><p className="mt-2 text-sm text-slate-400">Personalize apresentação e previsão sem mudar as chaves que preservam relatórios, IA e integrações.</p></div>{message ? <div className="rounded-2xl border border-sky-400/20 bg-sky-400/10 p-4 text-sm text-sky-100">{message}</div> : null}<AtlasCard><AtlasCardHeader eyebrow="Contrato canônico v1" title="Rótulos, ordem, visibilidade e probabilidade" description="Ganhos, perdas e compradores externos mantêm significado fixo. Alterações ficam limitadas à sua organização." action={<AtlasBadge tone="success">9 ETAPAS</AtlasBadge>} /><div className="space-y-3 p-5 sm:p-6">{stages.map((stage, index) => <article key={stage.key} className="grid gap-3 rounded-2xl border border-white/[.07] bg-white/[.025] p-4 md:grid-cols-[110px_1fr_110px_90px_100px] md:items-center"><div><span className="text-[10px] uppercase tracking-wider text-slate-500">Chave fixa</span><strong className="mt-1 block text-xs text-cyan-200">{stage.key}</strong></div><input aria-label={`Rótulo de ${stage.key}`} className="rounded-xl border border-white/10 bg-slate-950 px-3 py-2 text-sm text-white" value={stage.label} onChange={(event) => setStages((current) => current.map((item, position) => position === index ? { ...item, label: event.target.value } : item))}/><label className="text-xs text-slate-400">Probabilidade<input aria-label={`Probabilidade de ${stage.key}`} type="number" min="0" max="100" className="mt-1 w-full rounded-xl border border-white/10 bg-slate-950 px-3 py-2 text-white" value={stage.probability} onChange={(event) => setStages((current) => current.map((item, position) => position === index ? { ...item, probability: Number(event.target.value) } : item))}/></label><label className="text-xs text-slate-400">Ordem<input aria-label={`Ordem de ${stage.key}`} type="number" min="1" max="999" className="mt-1 w-full rounded-xl border border-white/10 bg-slate-950 px-3 py-2 text-white" value={stage.position} onChange={(event) => setStages((current) => current.map((item, position) => position === index ? { ...item, position: Number(event.target.value) } : item))}/></label><label className="flex items-center gap-2 text-xs text-slate-300"><input type="checkbox" checked={stage.visible} onChange={(event) => setStages((current) => current.map((item, position) => position === index ? { ...item, visible: event.target.checked } : item))}/>No Kanban</label></article>)}</div><div className="flex flex-col gap-3 border-t border-white/[.06] p-5 sm:flex-row sm:items-center sm:justify-between sm:px-6"><p className="max-w-2xl text-[11px] leading-5 text-slate-500">A probabilidade alimenta forecast, não garante venda. Aliases históricos são normalizados na entrada e não criam colunas duplicadas.</p><button type="button" disabled={saving} onClick={() => void save()} className="atlas-button-primary">{saving ? "Salvando..." : "Salvar configuração"}</button></div></AtlasCard></div>;
}
