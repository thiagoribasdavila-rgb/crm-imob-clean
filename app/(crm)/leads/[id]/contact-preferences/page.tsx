"use client";
import Link from"next/link";import{useParams}from"next/navigation";import{useEffect,useState}from"react";import{supabase}from"@/lib/supabase";import{AtlasBadge,AtlasSkeleton}from"@/components/ui/AtlasUI";import{AtlasCard,AtlasCardHeader,AtlasMetric}from"@/components/ui/AtlasCard";type Preference={channel:string;consent_status:string;lawful_basis?:string;evidence?:string;preferred:boolean;allowed_start:string;allowed_end:string;allowed_days:number[];timezone:string;valid_until?:string|null};type Payload={lead:{id:string;name:string};preferences:Preference[];eligibility:Record<string,{eligible:boolean;reason:string}>};const channelLabels:Record<string,string>={whatsapp:'WhatsApp',email:'E-mail',phone:'Telefone',sms:'SMS'};
export default function ContactPreferencesPage(){const{id}=useParams<{id:string}>(),[data,setData]=useState<Payload|null>(null),[error,setError]=useState(''),[busy,setBusy]=useState('');async function auth(){const{data:s}=await supabase.auth.getSession();return s.session?.access_token}async function load(){const token=await auth();if(!token){setError('Sessão expirada.');return}const response=await fetch(`/api/v1/leads/${id}/contact-preferences`,{headers:{Authorization:`Bearer ${token}`}}),payload=await response.json();if(!response.ok)setError(payload.error?.message||payload.error||'Falha ao carregar preferências.');else setData(payload)}useEffect(()=>{void load()},[id]);async function save(preference:Preference,status:string){const lawfulBasis=status==='granted'?window.prompt('Base/autorização do contato:','Autorização expressa registrada durante atendimento.')||'':'';const evidence=window.prompt('Descreva quando e como a preferência foi confirmada:');if(!evidence)return;setBusy(preference.channel);const token=await auth(),response=await fetch(`/api/v1/leads/${id}/contact-preferences`,{method:'PUT',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({channel:preference.channel,status,lawfulBasis,evidence,preferred:preference.preferred,allowedStart:preference.allowed_start,allowedEnd:preference.allowed_end,allowedDays:preference.allowed_days,timezone:preference.timezone,validUntil:preference.valid_until||null})}),payload=await response.json();if(!response.ok)setError(payload.error?.message||payload.error||'Preferência não salva.');else await load();setBusy('')}return <div className="space-y-6 pb-10" data-phase="73-contact-consent-preferences"><section className="atlas-grid-glow rounded-[30px] border border-cyan-400/10 bg-gradient-to-br from-cyan-500/[.12] via-blue-500/[.07] to-violet-500/[.12] p-6 sm:p-8"><Link href={`/leads/${id}`} className="text-sm font-semibold text-sky-300">← Voltar à lead</Link><div className="mt-5 flex gap-2"><AtlasBadge tone="success">CONSENTIMENTO</AtlasBadge><AtlasBadge tone="info">CANAL E HORÁRIO</AtlasBadge><AtlasBadge tone="warning">OPT-OUT IMEDIATO</AtlasBadge></div><h1 className="mt-5 text-3xl font-semibold text-white sm:text-5xl">Como {data?.lead.name||'a lead'} prefere conversar?</h1><p className="mt-4 max-w-3xl text-sm leading-7 text-slate-300">Uma única regra governa corretor, WhatsApp e IA. Sem autorização ou fora do horário, o contato não é preparado nem enviado.</p></section>{error?<div className="rounded-2xl border border-rose-400/20 bg-rose-400/10 p-4 text-rose-200">{error}</div>:null}{data?<section className="grid gap-4 sm:grid-cols-3"><AtlasMetric label="Canais autorizados" value={Object.values(data.eligibility).filter(item=>item.eligible).length} detail="Consentimento e horário válidos" trend="AGORA" tone="green"/><AtlasMetric label="Bloqueados" value={Object.values(data.eligibility).filter(item=>!item.eligible).length} detail="IA e envio respeitam a decisão" trend="PROTEGIDO" tone="amber"/><AtlasMetric label="Fonte única" value="1" detail="Corretor e automações consultam igual" trend="CANÔNICO" tone="blue"/></section>:null}{!data&&!error?<AtlasSkeleton className="h-80 w-full"/>:null}<div className="grid gap-5 md:grid-cols-2">{data?.preferences.map(pref=>{const eligibility=data.eligibility[pref.channel];return <AtlasCard key={pref.channel}><AtlasCardHeader eyebrow={channelLabels[pref.channel]} title={eligibility?.eligible?'Contato permitido agora':'Contato bloqueado agora'} description={eligibility?.reason||'Aguardando avaliação'} action={<AtlasBadge tone={pref.consent_status==='granted'?'success':pref.consent_status==='denied'?'danger':'warning'}>{pref.consent_status}</AtlasBadge>}/><div className="space-y-4 p-5"><div className="grid grid-cols-2 gap-3"><label className="text-xs text-slate-400">Início<input type="time" value={pref.allowed_start.slice(0,5)} onChange={event=>setData(current=>current?{...current,preferences:current.preferences.map(item=>item.channel===pref.channel?{...item,allowed_start:event.target.value}:item)}:current)} className="mt-1 w-full rounded-xl border border-white/10 bg-[#0a1120] p-3 text-white"/></label><label className="text-xs text-slate-400">Fim<input type="time" value={pref.allowed_end.slice(0,5)} onChange={event=>setData(current=>current?{...current,preferences:current.preferences.map(item=>item.channel===pref.channel?{...item,allowed_end:event.target.value}:item)}:current)} className="mt-1 w-full rounded-xl border border-white/10 bg-[#0a1120] p-3 text-white"/></label></div><label className="flex gap-2 text-sm text-slate-300"><input type="checkbox" checked={pref.preferred} onChange={event=>setData(current=>current?{...current,preferences:current.preferences.map(item=>item.channel===pref.channel?{...item,preferred:event.target.checked}:item)}:current)}/> Canal preferido pelo cliente</label><div className="grid grid-cols-3 gap-2"><button disabled={busy===pref.channel} onClick={()=>void save(pref,'granted')} className="atlas-button-primary">Autorizar</button><button disabled={busy===pref.channel} onClick={()=>void save(pref,'denied')} className="atlas-button-secondary">Opt-out</button><button disabled={busy===pref.channel} onClick={()=>void save(pref,'unknown')} className="atlas-button-secondary">Revisar</button></div><p className="text-[11px] leading-5 text-slate-500">Autorizar exige base e evidência. Opt-out bloqueia imediatamente pendências e novos contatos do canal.</p></div></AtlasCard>})}</div></div>}
