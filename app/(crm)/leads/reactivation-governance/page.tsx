"use client";import Link from"next/link";import{useEffect,useState}from"react";import{supabase}from"@/lib/supabase";import{AtlasBadge,AtlasEmpty,AtlasSkeleton}from"@/components/ui/AtlasUI";import{AtlasCard,AtlasCardHeader,AtlasMetric}from"@/components/ui/AtlasCard";type Batch={id:string;name:string;status:string;quality_status:string;daily_cap:number;interval_seconds:number;maximum_attempts:number;cooling_off_hours:number;eligible_count:number;queued_count:number;deliveryRate:number;replyRate:number;stopRequired:boolean;paused_reason:string|null};type Payload={batches:Batch[];summary:{batches:number;running:number;paused:number;replies:number}};
export default function ReactivationGovernancePage(){const[data,setData]=useState<Payload|null>(null),[error,setError]=useState(""),[preview,setPreview]=useState<Record<string,unknown>|null>(null),[form,setForm]=useState({quality:"unknown",dailyCap:100,intervalSeconds:30,consent:true,template:true,contacts:100,officialApi:true});async function token(){return(await supabase.auth.getSession()).data.session?.access_token}async function load(){const t=await token();if(!t)return;const r=await fetch("/api/v1/crm/reactivation/governance",{headers:{Authorization:`Bearer ${t}`}}),p=await r.json();if(r.ok)setData(p.data);else setError(p.error?.message||p.error)}useEffect(()=>{void load()},[]);async function simulate(){const t=await token(),r=await fetch("/api/v1/crm/reactivation/governance",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${t}`},body:JSON.stringify(form)}),p=await r.json();if(r.ok)setPreview(p.data);else setError(p.error?.message||p.error)}return <div className="space-y-6 pb-10" data-phase="89-consented-reactivation-governance"><section className="atlas-grid-glow rounded-[30px] border border-emerald-400/10 bg-gradient-to-br from-emerald-500/[.1] via-blue-500/[.08] to-violet-500/[.1] p-6 sm:p-8"><div className="flex flex-wrap gap-2"><AtlasBadge tone="success">WHATSAPP OFICIAL</AtlasBadge><AtlasBadge tone="info">CONSENTIMENTO OBRIGATÓRIO</AtlasBadge><AtlasBadge tone="violet">RESPOSTA INTERROMPE CADÊNCIA</AtlasBadge></div><h1 className="mt-5 text-3xl font-semibold text-white sm:text-5xl">Reativar com relevância, respeito e controle</h1><p className="mt-4 max-w-3xl text-sm leading-7 text-slate-300">Bases antigas passam por deduplicação, histórico de número inválido, opt-out, qualidade do WhatsApp, aprovação humana e escolha de manter ou trocar o corretor quando houver experiência ruim.</p></section>{error?<div className="rounded-2xl border border-rose-400/20 bg-rose-400/10 p-4 text-rose-200">{error}</div>:null}{data?<section className="grid gap-4 sm:grid-cols-4"><AtlasMetric label="Bases" value={data.summary.batches} detail="Dentro do escopo" trend="RLS" tone="blue"/><AtlasMetric label="Em operação" value={data.summary.running} detail="Aprovadas ou enfileiradas" trend="OFICIAL" tone="green"/><AtlasMetric label="Pausadas" value={data.summary.paused} detail="Qualidade ou controle humano" trend="SEGURANÇA" tone="amber"/><AtlasMetric label="Respostas" value={data.summary.replies} detail="Cadência interrompida" trend="CONVERSÃO" tone="violet"/></section>:!error?<AtlasSkeleton className="h-64 w-full"/>:null}<section className="grid gap-6 xl:grid-cols-[.65fr_1.35fr]"><AtlasCard><AtlasCardHeader eyebrow="Simulador sem envio" title="Calibrar cadência" description="Confira o limite antes de pedir aprovação."/><div className="space-y-3 p-5"><select value={form.quality} onChange={e=>setForm({...form,quality:e.target.value})} className="w-full rounded-xl border border-white/10 bg-[#0a1120] p-3 text-white"><option value="green">Qualidade verde</option><option value="yellow">Qualidade amarela</option><option value="unknown">Ainda desconhecida</option><option value="red">Qualidade vermelha</option></select><label className="block text-xs text-slate-400">Limite diário solicitado<input type="number" value={form.dailyCap} onChange={e=>setForm({...form,dailyCap:Number(e.target.value)})} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.035] p-3 text-white"/></label><label className="block text-xs text-slate-400">Intervalo em segundos<input type="number" value={form.intervalSeconds} onChange={e=>setForm({...form,intervalSeconds:Number(e.target.value)})} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.035] p-3 text-white"/></label>{[["consent","Consentimento documentado"],["template","Template aprovado"],["officialApi","API oficial pronta"]].map(([k,l])=><label key={k} className="flex gap-2 text-sm text-slate-300"><input type="checkbox" checked={Boolean(form[k as keyof typeof form])} onChange={e=>setForm({...form,[k]:e.target.checked})}/>{l}</label>)}<button onClick={()=>void simulate()} className="atlas-button-primary w-full">Simular política</button>{preview?<pre className="max-h-64 overflow-auto rounded-xl bg-[#050a14] p-3 text-xs text-slate-400">{JSON.stringify(preview,null,2)}</pre>:null}</div></AtlasCard><AtlasCard><AtlasCardHeader eyebrow="Operação" title="Bases governadas" description="Qualidade, entrega, resposta e pausa em uma visão simples." action={<Link href="/leads/import" className="text-xs font-semibold text-sky-300">Importar base →</Link>}/><div className="space-y-3 p-5">{data?.batches.map(b=><article key={b.id} className="rounded-2xl border border-white/[.07] bg-white/[.025] p-4"><div className="flex flex-wrap items-center gap-2"><b className="text-white">{b.name}</b><AtlasBadge tone={b.quality_status==="green"?"success":b.quality_status==="red"?"danger":"warning"}>{b.quality_status}</AtlasBadge><AtlasBadge tone="info">{b.status}</AtlasBadge>{b.stopRequired?<AtlasBadge tone="danger">PAUSAR</AtlasBadge>:null}</div><div className="mt-4 grid grid-cols-2 gap-3 text-xs text-slate-400 sm:grid-cols-4"><span>{b.eligible_count} elegíveis</span><span>{b.daily_cap}/dia</span><span>{b.interval_seconds}s intervalo</span><span>{b.replyRate}% resposta</span></div><p className="mt-3 text-[11px] text-slate-500">Máximo {b.maximum_attempts} tentativas · espera {b.cooling_off_hours}h · {b.paused_reason||"sem bloqueio ativo"}</p></article>)}{data&&!data.batches.length?<AtlasEmpty title="Nenhuma base" description="Importe uma base autorizada para iniciar com segurança."/>:null}</div></AtlasCard></section></div>}
