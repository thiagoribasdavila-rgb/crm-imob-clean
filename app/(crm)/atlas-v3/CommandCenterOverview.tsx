"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { AtlasBadge } from "@/components/ui/AtlasUI";
import { AtlasCard, AtlasCardHeader, AtlasMetric } from "@/components/ui/AtlasCard";
import { supabase } from "@/lib/supabase";

type Payload={status:"ready"|"blocked"|"unknown";generatedAt:string;health:{databaseOk:boolean;databaseLatencyMs:number;uptimeSeconds:number;environment:string;hosting:string};security:{publicSecretExposure:number;valuesReturned:false;governed:boolean};integrations:Record<string,{state:"configured"|"pending";realTestRequired:boolean}>;queues:{tasks:number|null;approvals:number|null;pendingOutbox:number|null;failedOutbox:number|null};resilience:{restorePassed:number|null;restorePendingOrFailed:number|null};homologation:{passed:number|null;failed:number|null};ai:{calls30d:number;tokens30d:number;estimatedCostUsd30d:number;averageLatencyMs30d:number;measured:boolean};critical:Record<string,boolean|null>};
const labels:Record<string,string>={database:"Banco",https:"HTTPS",workerSecret:"Workers",failedOutbox:"Fila sem falhas",restoreEvidence:"Restauração"};

export function CommandCenterOverview(){const[data,setData]=useState<Payload|null>(null);const[error,setError]=useState("");useEffect(()=>{let active=true;void supabase.auth.getSession().then(async({data:session})=>{const response=await fetch("/api/v1/governance/command-center",{cache:"no-store",headers:{Authorization:`Bearer ${session.session?.access_token||""}`}});const body=await response.json();if(!active)return;if(response.status===403){setError("Visão executiva disponível somente para a diretoria.");return}if(!response.ok){setError(body.error?.message||"Command Center indisponível.");return}setData(body.data)});return()=>{active=false}},[]);if(error)return <div className="rounded-2xl border border-white/[.07] bg-white/[.025] p-5 text-sm text-slate-400">{error}</div>;return <section className="space-y-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="atlas-page-eyebrow">Fase 10 · visão executiva</p><h2 className="mt-1 text-2xl font-semibold text-white">Command Center consolidado</h2></div><AtlasBadge tone={data?.status==="ready"?"success":data?.status==="blocked"?"danger":"warning"}>{data?.status==="ready"?"PRONTO":data?.status==="blocked"?"BLOQUEADO":"CARREGANDO / SEM EVIDÊNCIA"}</AtlasBadge></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><AtlasMetric label="Banco" value={data?`${data.health.databaseLatencyMs} ms`:"—"} detail={data?.health.databaseOk?"Consulta aprovada":"Sem evidência"} trend="HEALTH" tone={data?.health.databaseOk?"green":"amber"}/><AtlasMetric label="Fila de integração" value={data?.queues.pendingOutbox??"—"} detail={`${data?.queues.failedOutbox??"—"} falhas/dead letter`} trend="OUTBOX" tone={data?.queues.failedOutbox===0?"green":"amber"}/><AtlasMetric label="Custo IA · 30 dias" value={data?.ai.measured?`US$ ${data.ai.estimatedCostUsd30d.toFixed(2)}`:"—"} detail={`${data?.ai.calls30d??0} chamadas medidas`} trend="AI" tone="blue"/><AtlasMetric label="Homologação" value={data?.homologation.passed??"—"} detail={`${data?.homologation.failed??"—"} reprovações`} trend="GATE" tone={data?.homologation.failed===0?"green":"amber"}/></div><div className="grid gap-4 lg:grid-cols-[1.15fr_.85fr]"><AtlasCard><AtlasCardHeader eyebrow="Gates críticos" title="O que bloqueia produção" description="Ausência de evidência nunca aparece como aprovado."/><div className="grid gap-3 p-5 sm:grid-cols-2 sm:p-6">{Object.entries(data?.critical??{}).map(([key,value])=><div key={key} className="flex items-center justify-between rounded-2xl border border-white/[.07] bg-white/[.025] p-4"><span className="text-sm text-white">{labels[key]||key}</span><AtlasBadge tone={value===true?"success":value===false?"danger":"warning"}>{value===true?"OK":value===false?"BLOQUEIO":"SEM EVIDÊNCIA"}</AtlasBadge></div>)}</div></AtlasCard><AtlasCard><AtlasCardHeader eyebrow="Ações da diretoria" title="Atalhos operacionais" description="Cada detalhe permanece no módulo responsável."/><div className="grid gap-3 p-5 sm:p-6">{[["Segurança e segredos","/atlas-v3/governance"],["Saúde Hostinger","/integrations/hostinger"],["Backups e rollback","/atlas-v3/audit"],["Homologação por perfil","/atlas-v3/homologation"],["Custos e modelos de IA","/settings/ai"],["Aprovações pendentes","/approvals"]].map(([label,href])=><Link key={href} href={href} className="flex items-center justify-between rounded-2xl border border-white/[.07] bg-white/[.025] px-4 py-3 text-sm text-slate-300 transition hover:border-sky-400/20 hover:text-white"><span>{label}</span><span>→</span></Link>)}</div></AtlasCard></div><div className="text-right text-[11px] text-slate-600">{data?`Atualizado em ${new Date(data.generatedAt).toLocaleString("pt-BR")} · ${data.health.hosting} · ${data.health.environment}`:"Consolidando evidências reais…"}</div></section>}
