"use client";

import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";

type Lead={id:string;name:string|null;score:number|null;temperature:string|null;budget_max:number|null};
type Property={id:string;title:string|null;price:number|null;city:string|null;status:string|null};
export default function DigitalTwinPage(){const[leads,setLeads]=useState<Lead[]>([]);const[properties,setProperties]=useState<Property[]>([]);useEffect(()=>{void(async()=>{const[a,b]=await Promise.all([supabase.from("leads").select("id,name,score,temperature,budget_max").limit(12),supabase.from("properties").select("id,title,price,city,status").limit(12)]);setLeads((a.data??[])as Lead[]);setProperties((b.data??[])as Property[])})();},[]);return <div className="space-y-6"><header><p className="text-sm uppercase tracking-[.2em] text-violet-400">Digital Twin</p><h1 className="mt-2 text-3xl font-black">Gêmeos digitais do mercado</h1><p className="mt-2 text-zinc-400">Modelos vivos de compradores, imóveis e oportunidades para simulação de decisões.</p></header><div className="grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-5"><h2 className="font-bold">Compradores modelados</h2><div className="mt-4 space-y-3">{leads.map(l=><div key={l.id} className="rounded-xl border border-zinc-800 p-4"><div className="flex justify-between"><p className="font-semibold">{l.name||'Sem nome'}</p><span className="text-xs text-fuchsia-300">Score {l.score??0}</span></div><p className="mt-1 text-xs text-zinc-500">{l.temperature||'sem temperatura'} · orçamento {l.budget_max?Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(l.budget_max):'não informado'}</p></div>)}</div></section><section className="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-5"><h2 className="font-bold">Ativos modelados</h2><div className="mt-4 space-y-3">{properties.map(p=><div key={p.id} className="rounded-xl border border-zinc-800 p-4"><div className="flex justify-between"><p className="font-semibold">{p.title||'Imóvel'}</p><span className="text-xs text-emerald-300">{p.status}</span></div><p className="mt-1 text-xs text-zinc-500">{p.city||'Local não informado'} · {p.price?Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(p.price):'sem preço'}</p></div>)}</div></section></div></div>;}