"use client";

import { useEffect, useMemo, useState } from "react";
import { supabase } from "@/lib/supabase";

type Property={id:string;title:string|null;price:number|null;city:string|null;area:number|null;status:string|null};
export default function InvestorPage(){const[data,setData]=useState<Property[]>([]);useEffect(()=>{void(async()=>{const{data}=await supabase.from("properties").select("id,title,price,city,area,status").limit(40);setData((data??[])as Property[])})();},[]);const ranked=useMemo(()=>data.map(p=>({...p,yieldScore:Math.max(0,Math.min(100,70-(Number(p.price||0)/100000)+Number(p.area||0)/2))})).sort((a,b)=>b.yieldScore-a.yieldScore).slice(0,12),[data]);return <div className="space-y-6"><header><p className="text-sm uppercase tracking-[.2em] text-lime-400">Investor Intelligence</p><h1 className="mt-2 text-3xl font-black">Radar do investidor</h1><p className="mt-2 text-zinc-400">Ranking inicial de ativos para análise de preço, área, liquidez e potencial.</p></header><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{ranked.map(p=><article key={p.id} className="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-5"><div className="flex justify-between"><h2 className="font-bold">{p.title||'Ativo'}</h2><span className="text-xs text-lime-300">Score {p.yieldScore.toFixed(0)}</span></div><p className="mt-2 text-sm text-zinc-500">{p.city||'Sem cidade'} · {p.area??0} m²</p><p className="mt-4 text-xl font-black">{p.price?Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(p.price):'Sob consulta'}</p></article>)}</section></div>}