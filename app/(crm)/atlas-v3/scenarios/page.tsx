"use client";

import { useState } from "react";
export default function ScenariosPage(){const[leads,setLeads]=useState(500);const[cpl,setCpl]=useState(45);const[conversion,setConversion]=useState(1);const[ticket,setTicket]=useState(500000);const investment=leads*cpl;const sales=leads*conversion/100;const vgv=sales*ticket;const roi=investment?((vgv-investment)/investment)*100:0;const money=(n:number)=>Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(n);return <div className="space-y-6"><header><p className="text-sm uppercase tracking-[.2em] text-purple-400">Simulation Engine</p><h1 className="mt-2 text-3xl font-black">Cenários comerciais</h1></header><section className="grid gap-4 md:grid-cols-4">{[['Leads',leads,setLeads],['CPL',cpl,setCpl],['Conversão %',conversion,setConversion],['Ticket médio',ticket,setTicket]].map(([label,value,setter])=><label key={String(label)} className="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-5"><span className="text-sm text-zinc-500">{label as string}</span><input type="number" value={value as number} onChange={e=>(setter as (n:number)=>void)(Number(e.target.value))} className="mt-3 w-full rounded-xl border border-zinc-700 bg-zinc-950 p-3"/></label>)}</section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Investimento',money(investment)],['Vendas previstas',sales.toFixed(1)],['VGV previsto',money(vgv)],['ROI potencial',`${roi.toFixed(0)}%`]].map(([k,v])=><article key={k} className="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-5"><p className="text-sm text-zinc-500">{k}</p><p className="mt-2 text-2xl font-black">{v}</p></article>)}</section></div>}