type LeadContext={stage?:unknown;purpose?:unknown;nextActionStatus?:unknown;behavior?:{visits?:number;proposals?:number};dataQualityPercent?:number;score?:number};const stageAction=(stage:string,lead:LeadContext)=>stage==="proposta"||stage==="contrato"?"follow_proposal":stage==="visita"?"confirm_visit":(lead.behavior?.visits||0)>0?"present":stage==="qualificacao"?"present":stage==="contato"?"qualify":"contact";export function structuredMemoryFromGovernedContext(sections:Record<string,unknown>){const lead=(sections.lead||{})as LeadContext,project=(sections.project||null)as Record<string,unknown>|null,stage=String(lead.stage||"novo").toLowerCase(),intent=["moradia","investimento","renda","revenda"].includes(String(lead.purpose))?String(lead.purpose):"unknown",signals=[Number(lead.score)>=70?"score_high":Number(lead.score)<35?"score_low":null,["visita","proposta","contrato"].includes(stage)?"stage_advanced":null,(lead.behavior?.visits||0)>0?"visit_signal":null,(lead.behavior?.proposals||0)>0?"proposal_signal":null,Number(lead.dataQualityPercent)>=80?"data_quality_high":Number(lead.dataQualityPercent)<50?"data_quality_low":null,lead.nextActionStatus==="overdue"?"next_action_overdue":null].filter((value):value is string=>Boolean(value));return{intentKey:intent,timelineKey:"unknown",financingKey:"unknown",objectionKeys:[]as string[],signalKeys:[...new Set(signals)],stageKey:stage,actionKey:stageAction(stage,lead),developmentPresent:Boolean(project)}}
