export type ScenarioType="rule_default"|"higher_down_payment"|"shorter_term";export function calculatePaymentScenario(input:{price:number;ruleDownPercent:number|null;ruleInstallments:number|null;scenarioType:ScenarioType;requestedDownPercent?:number|null;requestedInstallments?:number|null}){const{price,ruleDownPercent,ruleInstallments,scenarioType}=input;if(!Number.isFinite(price)||price<=0)throw new Error("simulation_price_invalid");let downPercent=ruleDownPercent,installments=ruleInstallments;if(scenarioType==="higher_down_payment"){const requested=Number(input.requestedDownPercent);if(ruleDownPercent===null||!Number.isFinite(requested)||requested<ruleDownPercent||requested>90)throw new Error("simulation_down_outside_rule");downPercent=requested}if(scenarioType==="shorter_term"){const requested=Number(input.requestedInstallments);if(!ruleInstallments||!Number.isInteger(requested)||requested<1||requested>ruleInstallments)throw new Error("simulation_term_outside_rule");installments=requested}const down=downPercent===null?null:Math.round(price*downPercent)/100,balance=down===null?null:Math.round((price-down)*100)/100,installment=balance!==null&&installments?Math.round(balance/installments*100)/100:null;return{scenarioType,price,downPercent,down,balance,installments,installment,formula:"entrada = preço × percentual; saldo = preço − entrada; parcela linear = saldo ÷ quantidade",assumptions:["Valores sem correção monetária, juros, taxas, reforços ou análise de crédito.","A regra ativa define os limites; a incorporadora confirma o fluxo oficial.","Estoque, preço e vigência precisam ser reconfirmados antes da proposta."],requiresHumanApproval:true,isProposal:false,creditApproved:false}}
