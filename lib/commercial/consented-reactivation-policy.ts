export type Quality="green"|"yellow"|"red"|"unknown";const limits:Record<Quality,{dailyCap:number;minimumIntervalSeconds:number}>={green:{dailyCap:250,minimumIntervalSeconds:45},yellow:{dailyCap:100,minimumIntervalSeconds:90},unknown:{dailyCap:50,minimumIntervalSeconds:120},red:{dailyCap:0,minimumIntervalSeconds:3600}};export function evaluateReactivationPlan(input:{quality:Quality;requestedDailyCap:number;requestedIntervalSeconds:number;consentBasis?:string|null;approvedTemplate:boolean;eligibleContacts:number;officialApiReady:boolean}){const limit=limits[input.quality],blockers=[!input.consentBasis?.trim()?"consent_missing":null,!input.approvedTemplate?"approved_template_missing":null,input.eligibleContacts<1?"eligible_contacts_missing":null,!input.officialApiReady?"official_whatsapp_api_missing":null,input.quality==="red"?"quality_red":null].filter((v):v is string=>Boolean(v)),dailyCap=Math.max(0,Math.min(Math.floor(input.requestedDailyCap),limit.dailyCap)),intervalSeconds=Math.max(Math.floor(input.requestedIntervalSeconds),limit.minimumIntervalSeconds);return{eligible:blockers.length===0,dailyCap,intervalSeconds,sendWindow:{timezone:"America/Sao_Paulo",start:"09:00",end:"18:00"},maximumAttempts:2,coolingOffHours:72,stopOnReply:true,stopOnOptOut:true,stopOnInvalidPhone:true,oneLeadOneBroker:true,brokerChoiceOnBadExperience:true,officialApiOnly:true,humanApprovalRequired:true,blockers}}
